import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Cores primárias
  static const Color primary = Color(0xFF9399F9);
  static const Color secondary = Color(0xFF5F6CFA);
  static const Color tertiary = Color(0xFFB0B0B0);

  // Cores de texto
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Colors.grey;
  static const Color textLight = Colors.white;

  // Cores de fundo
  static const Color background = Color(0xFFF6F6F6);
  static const Color cardBackground = Color(0xFFF5F5F5);
  static const Color surfaceWhite = Colors.white;

  // Cores de campo de texto
  static const Color textFieldBackground = Color(0xFFE5E5E5);
  static const Color textFieldBorder = Colors.transparent;

  // Cores de estado
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF2196F3);
}
